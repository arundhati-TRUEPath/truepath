'use client';

interface PillButtonProps {
  children: React.ReactNode;
  pressed?: boolean;
  onClick?: () => void;
  compact?: boolean;
}

export default function PillButton({ children, pressed, onClick, compact }: PillButtonProps) {
  return null;
}
