import type { TagTone } from '@/lib/types/pathways';

interface TagProps {
  children: React.ReactNode;
  tone?: TagTone;
}

export default function Tag({ children, tone }: TagProps) {
  return null;
}
